# M0 parity baseline validation — 2026-07-20

## Decision

M0 baseline and contract implementation gates pass on this Linux host. This record proves the
post-parity-spec regression baseline used to begin M1; it is not final release qualification.

## Candidate identity

The workspace has no Git `HEAD` and its files are untracked, so no commit identifier can be
truthfully recorded. `pnpm source:fingerprint` deterministically hashes every runtime source, test,
workflow, integration, build script, generated protocol client, lockfile, and root tool
configuration while excluding build output and planning documents. The exact tested candidate is
recorded below after the final rerun:

```text
algorithm=sha256 fileCount=422 digest=018ae433ec069810ace16ee904167dbcf9340515f46326982bf0610f25e1f286
```

Final qualification still requires an exact committed candidate as required by PAR-QUAL-007.

## Host

```text
Linux 6.18.37-1-lts x86_64 GNU/Linux
Node v22.22.3
pnpm 10.34.5
rustc 1.96.0 (ac68faa20 2026-05-25)
```

## Required M0 gates

| Gate                                    | Result | Evidence boundary                                                                                                                                                                        |
| --------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm generate:protocol`                | PASS   | 134 binding export tests passed; generated Rust/TypeScript boundary completed with exit 0.                                                                                               |
| `cargo test --workspace --all-features` | PASS   | All crate, integration, real-program, and doc tests completed with exit 0.                                                                                                               |
| `pnpm validate`                         | PASS   | Formatting, ESLint, Clippy with warnings denied, type checks, Rust/JS tests, and production builds completed with exit 0. Protocol client: 45 tests; desktop: 519 tests across 60 files. |

`ts-rs` emitted its existing warnings that it does not interpret selected Serde validation
attributes; strict runtime validation remains covered by Rust deserializers and paired Zod tests.
No gate failure was suppressed.

## M0 acceptance evidence

- **M0-AC-01:** [`CMUX_CAPABILITY_INVENTORY.md`](../CMUX_CAPABILITY_INVENTORY.md) classifies every
  observed Tier A capability and explicit non-goal against the pinned evidence register.
- **M0-AC-02:** [`PARITY_CONTRACT_MATRIX.md`](../PARITY_CONTRACT_MATRIX.md) assigns owner, trust
  boundary, compatibility, and test strategy to every proposed concept. ADRs 0001–0004 accept the
  current process, card-slot, attention, and capability-versioning decisions.
- **M0-AC-03:** The required protocol, Rust workspace, and full repository validation gates pass;
  existing workspace, terminal, browser, persistence, and notification suites remain green.

## Review boundary

M0 documents require independent review before later edits may rely on their classifications as
final. Individual capabilities already supported by the pre-existing application or the first M1
card-slot slice retain their Supported status; M1 as a complete milestone and all later milestones
remain incomplete exactly as recorded in the inventory.
